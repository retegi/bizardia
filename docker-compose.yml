services:
  web:
    build:
      context: .
      dockerfile: docker/django/Dockerfile
    container_name: bizardia_web
    volumes:
      - ./src:/app
      - ./staticfiles:/app/staticfiles
    env_file:
      - .env
